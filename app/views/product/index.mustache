{{> header }}

 <section class="section">
        <div class="container">

<table class="table">
    <thead>
        <tr>
            <th>Name</th>
            <th>Description</th>
            <th>Stock</th>
            <th>Price</th>
        </tr>
    </thead>
    {{# data.products }}
    <tbody>
        <tr>
            <td>{{ name }}</td>
            <td>{{ description }}
            <td>{{ stock }}
            <td>{{ price }}
            {{# data.user }}
             <td>
                <a class="button is-primary" href="product-form?id={{ product_id }}">Edit</a>
                <a class="button is-danger" href="delete-product?id={{ product_id }}"">Delete</a>
             </td>
            {{/ data.user}}
    {{/ data.products }}
        </tr>
    </tbody>
</table>

    </container
</section>

{{> footer }}